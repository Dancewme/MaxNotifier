# TODO — MaxNotifier

## Что сделано

- [x] Изучен API MAX — подтверждено, что бот-API не подходит (бот видит только свои чаты)
- [x] Выбран подход: мониторинг окон уведомлений десктоп-клиента MAX через Win32 SetWinEventHook
- [x] Создан `max_monitor.py` — мониторинг через ctypes (без сторонних библиотек)
- [x] Обновлён `config.py` — `[MAX] TOKEN` заменён на `[MAX] PROCESS_NAME` + `DEBUG`
- [x] Обновлён `app.py` — использует `MaxMonitor` вместо `MaxClient`
- [x] Переписан `README.md` под новый подход
- [x] Удалён устаревший `max_client.py`
- [x] `build.bat` исправлен (UTF-8 + CRLF, pyinstaller в одну строку, `python -m PyInstaller`)
- [x] `build.bat` создаёт `config.ini` в `dist\` рядом с exe
- [x] Тестирование на реальном MAX — уведомления ловятся корректно
- [x] Добавлен `EVENT_OBJECT_SHOW` — ловля показа скрытых окон уведомлений
- [x] Фильтрация Qt-мусора (класс `QEventDispatcherWin32*`)
- [x] Проверка PID процесса в callback
- [x] Дедупликация SHOW-событий по времени (5 сек)
- [x] Сборка exe — работает без ошибок
- [x] README.md обновлён под финальное состояние

## Возможные улучшения (на будущее)

- [ ] Фильтрация по размеру окна (уведомления обычно маленькие)
- [ ] Логирование имени отправителя/текста сообщения (если доступно из заголовка окна)
- [ ] Поддержка нескольких получателей email
- [ ] Минификация exe (убрать pygame из окружения сборки)